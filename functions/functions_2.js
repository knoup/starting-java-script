// fuction expression
// function anonymous

// parâmetros (parameters)
const sum = function(number1, number2) {
    let total = number1 + number2
    return total
}

let number1 = 10
let number2 = 20
console.log(`A soma de ${number1} com ${number2} é ${sum(number1, number2)}`)
